using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public abstract class InteractableObject : MonoBehaviour
{
    //// General method to get player in interactable object
    //protected bool TryGetPlayer(string playerID, out PlayerController player)
    //{
    //    player = PlayerManager.Instance.GetPlayer(playerID);
    //    if (player == null)
    //    {
    //        Debug.Log($"PlayerID '{playerID}' doesn't exist!");
    //        return false;
    //    }
    //    return true;
    //}
}
